// engine/renderer.js — rendering helpers (placeholder)
export function renderFrame(ctx, state) {
  console.warn('engine/renderer.renderFrame() stub — implement draw logic');
}

export function resizeCanvas(canvas, layoutInfo) {
  console.warn('engine/renderer.resizeCanvas() stub');
}
